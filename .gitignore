.DS_Store
*/.DS_Store
bin/
dist/
/ops-repo-mcp
# stray binaries from before the rename, so they stay ignored rather than
# becoming untracked files the next `git add -A` would sweep in
/mcp-server
cover.out
.env
.mcp.json
# the plugin ships its own Claude Code MCP declaration — it is not local config
!agents-plugin/.mcp.json
CLAUDE.local.md
events.jsonl
.claude
.agent
